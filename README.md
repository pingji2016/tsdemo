# tsdemo
# Todo App - TypeScript 全栈应用

一个使用 TypeScript 构建的现代化待办事项应用，包含前端 React 应用和后端 Express 服务。

## 📋 功能特性

### 核心功能
- ✅ **创建待办事项** - 添加标题和可选的描述
- 📋 **查看所有待办** - 清晰列出所有待办事项
- ✏️ **更新待办状态** - 标记完成/未完成
- 🗑️ **删除待办事项** - 移除不需要的事项

### 技术特性
- 🔒 **TypeScript 类型安全** - 前后端完整的类型定义
- 🎯 **错误处理** - 友好的错误提示
- ⏳ **加载状态** - 直观的加载指示器
- 🎨 **响应式设计** - 适配各种设备

## 🛠️ 技术栈

### 前端
- React 18
- TypeScript 5
- Axios (HTTP 请求)
- CSS3 (响应式设计)

### 后端
- Node.js
- Express 4
- TypeScript 5
- CORS (跨域支持)

## 📁 项目结构

```
todo-app/
├── server/                 # 后端服务
│   ├── src/
│   │   ├── models/        # 数据模型
│   │   ├── routes/        # API 路由
│   │   └── index.ts       # 入口文件
│   ├── package.json
│   └── tsconfig.json
│
└── client/                 # 前端应用
    ├── public/
    │   └── index.html
    ├── src/
    │   ├── components/     # React 组件
    │   ├── types/          # TypeScript 类型
    │   ├── api/            # API 调用
    │   ├── App.tsx
    │   └── index.tsx
    ├── package.json
    └── tsconfig.json
```

## 🚀 快速开始

### 前置要求
- Node.js (v14 或更高)
- npm 或 yarn

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd todo-app
```

2. **安装后端依赖**
```bash
cd server
npm install
```

3. **安装前端依赖**
```bash
cd ../client
npm install
```

### 运行应用

1. **启动后端服务** (端口 3001)
```bash
cd server
npm run dev
```

2. **启动前端应用** (端口 3000)
```bash
cd client
npm start
```

3. **访问应用**
   打开浏览器访问 `http://localhost:3000`

## 📡 API 接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/todos` | 获取所有待办 |
| GET | `/api/todos/:id` | 获取单个待办 |
| POST | `/api/todos` | 创建待办 |
| PUT | `/api/todos/:id` | 更新待办 |
| DELETE | `/api/todos/:id` | 删除待办 |

## 🎯 使用示例

### 创建待办
```typescript
// 发送请求
const newTodo = await todoApi.createTodo({
  title: "学习 TypeScript",
  description: "完成全栈项目",
  completed: false
});
```

### 更新状态
```typescript
// 标记完成
await todoApi.updateTodo(todoId, { completed: true });
```

## 🔧 配置说明

### 后端配置
- 默认端口：3001
- API 基础路径：`/api`
- 跨域支持：允许所有来源

### 前端配置
- 默认端口：3000
- API 代理：自动转发到后端
- 支持热重载

## 📝 待办清单

- [ ] 添加用户认证
- [ ] 数据持久化（数据库）
- [ ] 待办事项分类
- [ ] 截止日期功能
- [ ] 搜索和筛选
- [ ] 单元测试

## 🤝 贡献指南

1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 📄 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 👥 作者

Your Name - [@yourtwitter](https://twitter.com/yourtwitter)

## 🙏 致谢

- React 团队
- TypeScript 团队
- 所有贡献者

---

**项目状态**: 开发中 🚧